/*
Problem Statement: Given an array, we have to find the largest element in the array.

Example 1:
Input: arr[] = {2,5,1,3,0};
Output: 5
Explanation: 5 is the largest element in the array. 

*/

// method 1: sort the array in ascending order the last element will be the largest

// method 2: initialize max=arr[0], pass through the array each time check if its greater then the the current max, if yes then max=arr[i] else continue

int largest(int arr[],int n){
  int max=arr[0];
  for(int i=1; i<n ; i++){
  if(arr[i] > max){
  max=arr[i];
  }
  }
  return max;
}
